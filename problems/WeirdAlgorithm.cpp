#include <iostream>
#include <algorithm>
#include <string>
#include <vector>
#include <queue>
#include <deque>
#include <stack>
#include <set>
#define FIO ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
using namespace std;
#define el "\n"
typedef long long ll;
typedef long double ld;

void solve(){
  
}


int main() {
    FIO
    int m;
cin >> m;
while(true) {
if(m==1)break;
    if(m%2==0)cout<<(m/2)<<el;

    else cout<< (m*3)+1<<el;
    
 
    
    return 0;
}
}